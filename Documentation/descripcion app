
una app para gestion de notas, promedio, correlativas de una carrera
funciones principales:
* listar materias de una carrera (pantalla TODAS)
    lista de botones, uno por materia
    estos botones deben poder colorearse segun la disponibilidad (verde, azul, amarillo, blanco), determinada por el sistema
    al pulsar una materia, abre un panel flotante que permite modificar esa materia (elegir nota y condicion), boton aceptar y cancelar
    al pulsar aceptar, se llama al backend para que actualice la DB registrarInscripcion(int ordenMateria, int nota, int idCondicion)
    disponer de una funcion actualizar
* listar materias cursadas (pantalla CURSADAS)

*

